#include<iostream>
#include<cassert>
using namespace std;
struct node {
   int data;
   node *nxt=0;
};
struct linked list {
    node *head;
    int get(int pos){

    }
};
int main() { 
  node *N=new node(); 
  N->data=1;
 // assert(N->nxt==0);
 return 0;
}